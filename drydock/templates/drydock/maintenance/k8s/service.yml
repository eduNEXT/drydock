---
apiVersion: v1
kind: Service
metadata:
  name: maintenance-caddy
  labels:
    app.kubernetes.io/name: maintenance-caddy
spec:
  type: ClusterIP
  ports:
    - port: 80
      name: http
  selector:
    app.kubernetes.io/name: maintenance-caddy
