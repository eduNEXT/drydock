---
apiVersion: v1
kind: Service
metadata:
  name: edge-proxy
  labels:
    app.kubernetes.io/name: edge-proxy
spec:
  type: ClusterIP
  ports:
    - port: 80
      name: http
  selector:
    app.kubernetes.io/name: edge-proxy
